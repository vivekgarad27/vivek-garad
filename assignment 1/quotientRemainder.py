numerator = int(input("Enter numerator:"))
denominator = int(input("Enter denominator:"))

numerator = numerator // denominator
rumerator = numerator % denominator

print(f"quotient: , {quotient}" )
print(f"remainder: , {remainder}")

